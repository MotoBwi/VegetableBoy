interface:
  display_name: "VegetableBoy"
  short_description: "Repo-specific patterns and workflows for VegetableBoy"
  default_prompt: "Use the VegetableBoy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true